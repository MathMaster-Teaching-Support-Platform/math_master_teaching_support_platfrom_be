package com.fptu.math_master.enums;

/**
 * Direction of a cash flow entry.
 */
public enum CashFlowType {
    /** Money coming into the platform (deposits, course sales, subscriptions). */
    INFLOW,
    /** Money leaving the platform (withdrawals, refunds, commissions paid out). */
    OUTFLOW
}
